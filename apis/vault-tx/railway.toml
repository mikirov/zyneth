[build]
builder = "DOCKERFILE"
dockerfilePath = "./apis/vault-tx/Dockerfile"
watchPatterns = [
    "apis/vault-tx/**",
    "!/*.md",
    "bun.lock",
    "package.json",
]

[deploy]
numReplicas = 1
healthcheckPath = "/health"
sleepApplication = true
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
region = "europe-west4"
